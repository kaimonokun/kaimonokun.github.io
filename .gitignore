# ==========================================================
# GitHub Pages に「公開してはいけない」ファイル
# ==========================================================

# 古いパスワードが平文で残っている会話ログ（絶対に公開しない）
要件定義.txt

# ログイン情報（認証は Vercel に移行済み。念のため二重で除外）
data/users.json

# 内部資料の PDF（公開したいものがあれば、あとで個別に許可する）
*.pdf

# ==========================================================
# OS やエディタが自動で作るファイル
# ==========================================================
.DS_Store
Thumbs.db
desktop.ini

# ==========================================================
# Node.js（このフォルダでは使わないが、念のため）
# ==========================================================
node_modules/
